version: '2'
services:
  telegram-bot:
    build: telegram-bot
    ports:
      - "8081:8081"
  backend:
    build: backend
    ports:
      - "8080:8080"
  frontend:
    build: frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/src:/code/src
      - /project/node_modules
    depends_on:
      - backend
